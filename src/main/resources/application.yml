
spring:
  application:
    name: AIFilterBox

  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: org.sqlite.JDBC
    url: "jdbc:sqlite:src/main/resources/db/ai-filter-box.db"

  rabbitmq:
    addresses: hk.hjong.cn:5672
    username: Chicring
    password: 95aninjv

  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
